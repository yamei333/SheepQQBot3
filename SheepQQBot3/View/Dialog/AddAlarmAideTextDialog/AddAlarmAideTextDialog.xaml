<local:AddDialogWindowBase x:Class="SheepQQBot3.View.AddAlarmAideTextDialog"
                           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                           xmlns:local="clr-namespace:SheepQQBot3.View"
                           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                           Title="提醒内容"
                           Width="400"
                           Height="138"
                           x:TypeArguments="local:AddAlarmAideTextDialogViewModel"
                           TextOptions.TextFormattingMode="Display"
                           TextOptions.TextRenderingMode="ClearType"
                           WindowStartupLocation="CenterOwner"
                           WindowStyle="ToolWindow"
                           mc:Ignorable="d">
    <Window.DataContext>
        <local:AddAlarmAideTextDialogViewModel />
    </Window.DataContext>
    <Grid Margin="4,4,4,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="64" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.Resources>
            <Style x:Key="HeaderTextBlockStyle" TargetType="TextBlock">
                <Setter Property="Padding" Value="3,2,0,0" />
            </Style>
        </Grid.Resources>
        <TextBlock Style="{StaticResource HeaderTextBlockStyle}" Text="内容：" />
        <TextBox x:Name="TxtContent"
                 Grid.Row="0"
                 Grid.Column="1"
                 Height="64"
                 HorizontalContentAlignment="Left"
                 VerticalContentAlignment="Top"
                 AcceptsReturn="True"
                 KeyDown="OnCtrlEnter"
                 Style="{StaticResource HintTextStyle}"
                 Text="{Binding AlarmContent}"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Visible" />
        <Grid Grid.Row="1"
              Grid.Column="0"
              ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Click="OK_Click"
                    Content="确定"
                    Style="{StaticResource DialogButtonStyle}" />
            <Button Grid.Column="1"
                    Click="Cancel_Click"
                    Content="取消"
                    Style="{StaticResource DialogButtonStyle}" />
        </Grid>
    </Grid>
</local:AddDialogWindowBase>
